<div class="relative mb-5 w-full">
    <h1 class="text-lg font-semibold text-zinc-900 dark:text-zinc-50">{{ __('Settings') }}</h1>
    <p class="text-sm text-zinc-500 dark:text-zinc-400">{{ __('Manage your profile and account settings') }}</p>
</div>